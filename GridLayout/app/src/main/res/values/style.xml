<resources>

    <!-- Estilo para los botones en las dos primeras filas -->
    <style name="MyMaterialButtonStyle">
        <item name="android:backgroundTint">#F293B3</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:drawableStart">@mipmap/ic_launcher</item>
    </style>

    <!-- Estilo para los botones en las filas siguientes -->
    <style name="MyMaterialButtonStyleBis">
        <item name="android:backgroundTint">#03DAC5</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:radius">10dp</item>
    </style>

    <!-- Estilo para el visor -->
    <style name="MiVisorStyle">
        <item name="android:textColor">#0000FF</item>
        <item name="android:hint">@string/defecto</item>
        <item name="android:fontFamily">@fonts/colour.ttf</item>
    </style>

</resources>
