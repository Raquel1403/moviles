<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el switch está activado (ON) -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <corners android:radius="10dp" />
            <solid android:color="#4CAF50" /> <!-- Color de fondo del track cuando está ON -->
        </shape>
    </item>
    <!-- Estado cuando el switch está desactivado (OFF) -->
    <item android:state_checked="false">
        <shape android:shape="rectangle">
            <corners android:radius="10dp" />
            <solid android:color="#BDBDBD" /> <!-- Color de fondo del track cuando está OFF -->
        </shape>
    </item>
</selector>
